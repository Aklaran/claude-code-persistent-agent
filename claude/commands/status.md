Quick orientation. Read and summarize:

1. Today's session log: cat ~/.claude/memory/sessions/$(date +%Y-%m-%d).jsonl 2>/dev/null
2. If in a repo with .beads/: bd ready
3. Check cross-project beads if configured in MEMORY.md
